# Honest Haven

A safe space for community resources. This website lists LGBTQ/community supportive groups and movements.

## Getting Started

1.  Install dependencies:
    ```bash
    npm install
    ```

2.  Start the development server:
    ```bash
    npm run dev
    ```

3.  Build for production:
    ```bash
    npm run build
    ```

## Technologies

-   React
-   Vite
-   Tailwind CSS

## Deployment

This site is configured for deployment on **Netlify**.

### Deploy via Netlify UI:
1. Connect your GitHub repository to Netlify
2. Netlify will automatically detect the build settings from `netlify.toml`
3. Deploy!

### Build Settings (already configured in `netlify.toml`):
- **Build command:** `npm run build`
- **Publish directory:** `dist`

## Contributing

Feel free to add more resources to the list in `src/data/groups.js`.
